name: weather_mvvm_simple
description: "Weather app - Simple MVVM study project"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0


dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  geolocator: ^14.0.2
  http: ^1.6.0
  provider: ^6.1.5+1
  skeletonizer: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
